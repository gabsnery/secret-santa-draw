import React from "react";

const Form = () => {
    return <form>
        <input type="text" placeholder="Participant`s name"/>
        <button disabled>add</button>
    </form>
}
export default Form